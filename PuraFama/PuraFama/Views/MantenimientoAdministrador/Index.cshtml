@model IEnumerable<PuraFama.Models.administrador>

@{
    ViewBag.Title = "Index";
}

<h2>Inicio</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombre_admin)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombre_admin)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.rut_admin }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.rut_admin }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.rut_admin })
        </td>
    </tr>
}

</table>
